<Page x:Class="BookPlatformWPF.Pages.AdminPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Администрирование">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="⚙️ Администрирование" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <TabControl Grid.Row="1">

            <!-- Жалобы -->
            <TabItem Header="Жалобы">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="🔄 Обновить" HorizontalAlignment="Left"
                            Margin="0,5,0,5" Padding="8,4" Click="BtnRefreshComplaints_Click"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="ComplaintsPanel"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Заявки на разморозку -->
            <TabItem Header="Заявки на разморозку">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="🔄 Обновить" HorizontalAlignment="Left"
                            Margin="0,5,0,5" Padding="8,4" Click="BtnRefreshUnfreeze_Click"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="UnfreezePanel"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Заявки на роль Автора -->
            <TabItem Header="Заявки на автора">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="🔄 Обновить" HorizontalAlignment="Left"
                            Margin="0,5,0,5" Padding="8,4" Click="BtnRefreshRoles_Click"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="RoleRequestsPanel"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>

            <!-- Замороженные -->
            <TabItem Header="Замороженные">
                <ScrollViewer VerticalScrollBarVisibility="Auto">
                    <StackPanel x:Name="FrozenPanel" Margin="0,5"/>
                </ScrollViewer>
            </TabItem>

            <!-- Пользователи -->
            <TabItem Header="Пользователи">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                    </Grid.RowDefinitions>
                    <Button Content="🔄 Обновить" HorizontalAlignment="Left"
                            Margin="0,5,0,5" Padding="8,4" Click="BtnRefreshUsers_Click"/>
                    <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto">
                        <StackPanel x:Name="UsersPanel"/>
                    </ScrollViewer>
                </Grid>
            </TabItem>

        </TabControl>
    </Grid>
</Page>