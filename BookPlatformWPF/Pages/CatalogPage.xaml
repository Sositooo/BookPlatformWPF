<Page x:Class="BookPlatformWPF.Pages.CatalogPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="Каталог книг">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="📚 Каталог книг" FontSize="22" FontWeight="Bold" Margin="0,0,0,10"/>

        <!-- Фильтры -->
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="150"/>
                <ColumnDefinition Width="180"/>
            </Grid.ColumnDefinitions>
            <TextBox x:Name="TxtSearch" PlaceholderText="Поиск по названию / автору"
                     Height="30" Padding="5" TextChanged="Filter_Changed"/>
            <ComboBox x:Name="CmbSort" Grid.Column="1" Margin="5,0"
                      Height="30" SelectionChanged="Filter_Changed">
                <ComboBoxItem Content="По названию" IsSelected="True" Tag="title"/>
                <ComboBoxItem Content="По рейтингу"  Tag="rating"/>
            </ComboBox>
            <ComboBox x:Name="CmbGenre" Grid.Column="2"
                      Height="30" DisplayMemberPath="Name"
                      SelectedValuePath="GenreID"
                      SelectionChanged="Filter_Changed"/>
        </Grid>

        <!-- Сетка книг -->
        <ScrollViewer Grid.Row="2" VerticalScrollBarVisibility="Auto">
            <WrapPanel x:Name="BooksPanel" Margin="0,5"/>
        </ScrollViewer>
    </Grid>
</Page>